name: Production CI/CD

on:
    push:
        branches: [main]

env:
    AWS_REGION: us-east-1
    ENV: prod

jobs:
    test-and-deploy:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: '22'
                  cache: 'npm'

            - name: Setup Python for SAM CLI
              uses: actions/setup-python@v4
              with:
                  python-version: '3.11'

            - name: Setup SAM CLI
              uses: aws-actions/setup-sam@v2

            - name: Install dependencies
              run: npm ci

            - name: Code quality check (ESLint)
              run: npx eslint power-of-math/ --ext .mjs || echo "ESLint not configured, skipping..."

            - name: Run unit tests
              run: npm test
              env:
                  ENV: prod

            - name: Build SAM application
              run: sam build

            - name: Configure AWS credentials
              uses: aws-actions/configure-aws-credentials@v4
              with:
                  aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
                  aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
                  aws-region: ${{ env.AWS_REGION }}

            - name: Deploy to Production
              run: |
                  sam deploy \
                    --config-env prod \
                    --no-confirm-changeset \
                    --no-fail-on-empty-changeset

        # - name: Run QA tests
        #   run: |
        #     API_ENDPOINT=$(aws cloudformation describe-stacks --stack-name power-of-math-prod --query 'Stacks[0].Outputs[?OutputKey==`PowerOfMathApi`].OutputValue' --output text)
        #     echo "QA tests would run here against: $API_ENDPOINT"
